# Project Memory

## Goal

Create a ZCode plugin for macOS that mirrors the practical Codex Computer Use loop while granting the agent the broad local access selected by the user.

## Current architecture

- Distribution: repository-backed ZCode marketplace.
- Plugin: `plugins/macos-computer-use`.
- Agent guidance: `skills/macos-computer-use/SKILL.md`.
- Tool transport: two local stdio MCP servers.
- Primary implementation: signed `CuaDriver.app` 0.13.1, launched on a private per-user/version socket in unrestricted mode for background AX/pixel control without focus stealing; startup verifies exact app version, required tools, and the daemon's reported permission mode.
- Fallback implementation: repository-owned Python/PyObjC MCP using AppKit, Quartz, and macOS Accessibility for unrestricted direct foreground window and full-desktop control when the driver is unavailable or refuses an operation.
- Authorization boundary: ZCode Full Access plus macOS Accessibility and Screen Recording TCC grants.
- Privacy boundary: runtime screen, Accessibility, clipboard, and input payloads remain local; upstream Cua Driver telemetry is disabled by environment and a plugin-private persisted preference that never edits the user's unrelated `~/.cua-driver` config.

## Compatibility contract

The core tool names mirror Codex Computer Use: `list_windows`, `get_window`, `list_apps`, `launch_app`, `get_window_state`, `click`, `press_key`, `type_text`, `scroll`, `set_value`, `drag`, `perform_secondary_action`, and `activate_window`.

## Product decisions

- Ship both a ZCode plugin manifest and a Codex-compatible manifest so the same source can be imported by either host.
- Do not implement OpenAI's action confirmation taxonomy or Windows target denials in this plugin.
- Retain correctness constraints: select exactly one returned window, invalidate stale Accessibility indexes after observation changes, validate screenshot IDs when supplied, and refresh after actions.
- Add raw cursor, mouse button, clipboard, health, permission, and fresh-screenshot-bound full-desktop tools beyond the compatibility core.
- Prefer the Cua Driver background ladder (`AX -> pixel -> foreground`) and use the repository-owned direct backend as the final no-approval fallback.
- Pin the upstream universal release by tag and SHA-256; publish it only in the plugin data directory, bind reuse to the Cua AI signer identity and tested tool surface, and prove the dedicated daemon reports unrestricted mode.

## Verification status

- 2026-07-28: official ZCode Skill, Plugin, marketplace, MCP, and Full Access documentation reviewed.
- 2026-07-28: bundled Codex Computer Use `guidance.md`, `api.md`, and `confirmations.md` reviewed; tool contract frozen.
- 2026-07-28: historical Hermes `macos-computer-use` Skill at revision `17dfc6bec4a8b7fd840d479c33e9a7b2449f805d` and Cua Driver 0.12.6 implementation reviewed; background control adopted as the primary architecture.
- 2026-07-28: independent first-use Agent forward test passed after clarifying desktop/window routing, permission order, shortcut shapes, and sessionless fallback behavior.
- 2026-07-28: local Skill/contract/MCP stdio tests and GitHub Actions on Windows plus macOS passed, including native AppKit/ApplicationServices/Quartz imports and the pinned release checksum.
- 2026-07-28: optimization audit hardened crash recovery and primary daemon identity/mode verification, then added a 28-tool direct fallback with full-desktop screenshot-bound mouse/keyboard control.
- 2026-07-28: added an opt-in disposable AppKit live smoke gate covering signed-driver attribution, primary background screenshot/type/click, fallback desktop keyboard delivery, native MCP image blocks, exact window binding, re-observation, visible verification, and cleanup without user-document mutation.
- 2026-07-28: pinned the 51 MB universal release archive and required a valid macOS code signature/Gatekeeper assessment before reuse; the later plugin-owned installer removes the upstream installer's global `/Applications` and daemon lifecycle side effects.
- 2026-07-28: direct desktop fallback now captures each active display separately and binds coordinates to that display's own screenshot dimensions, avoiding mixed Retina/non-Retina global-scale drift and rejecting layout changes before input.
- 2026-07-28: released the multi-display/runtime reproducibility batch as plugin 0.3.0; the fallback now requires CPython 3.10+, pins PyObjC 12.2.1 exactly, and accepts binary wheels only in install, first-run, and CI paths.
- YAML plain scalars ending a colon-bearing CLI token immediately before whitespace can invalidate an Actions workflow before jobs are created; quote the complete `run` value when using `--only-binary=:all:`.
- Apple's `lipo -verify_arch` syntax requires the input file before the command and architecture list (`lipo "$binary" -verify_arch arm64 x86_64`).
- 2026-07-28: comparison with the current Hermes/cua-driver macOS Skill exposed that `unrestricted` is still below configured Cua user/managed policy ceilings. Plugin 0.3.1 clears inherited policy variables for its dedicated launch and accepts the daemon only when status proves user, managed, and session policies are all absent.
- 2026-07-28: plugin 0.4.0 removed a fallback parity detour by returning matched pid/window handles from `launch_app`; plugin 0.8.0 later aligned `get_window_state` with the current Codex screenshot-only default while retaining explicit AX-only or combined observations.
- 2026-07-28: plugin 0.4.1 separates Accessibility readiness from pixel readiness. AX-only tasks can continue with `include_screenshot:false` when Screen Recording is absent; permission UX is deferred until the requested route genuinely needs screenshots, coordinates, or desktop state.
- 2026-07-28: plugin 0.4.2 closes the remaining PyObjC drift path discovered in the real macOS CI install log: `pyobjc-core` and `CoreText` are now explicit 12.2.1 pins alongside the three top-level frameworks, and every install uses `--no-deps --only-binary=:all:`.
- 2026-07-28: plugin 0.4.3 validates the actual PyObjC wheel range, CPython 3.10 through 3.15, before creating an environment; future unsupported interpreters fail with a precise diagnostic instead of a late pip resolution error.
- 2026-07-28: plugin 0.4.4 pins the 45 official PyPI SHA-256 values covering all normal and free-threaded CPython 3.10–3.15 variants for the complete five-package PyObjC closure; install, first-run, and CI now require hashes.
- 2026-07-28: plugin 0.5.0 makes fallback first-run crash-safe: a version-specific staging venv must install, import, and pass the MCP self-test before an atomic rename publishes it; interrupted environments are never treated as reusable.
- 2026-07-28: plugin 0.5.1 applies the screenshot handle's five-minute freshness ceiling to fallback AX element indexes too; expired native references are evicted and require re-observation instead of remaining actionable indefinitely.
- 2026-07-28: plugin 0.6.0 binds fallback screenshot IDs and AX element indexes to `(app, pid, window_id)`, so an app restart cannot reuse an old observation even if macOS recycles the numeric window ID.
- 2026-07-28: the agent workflow borrows three reliability lessons from existing macOS Computer Use skills without importing their safety gates: visible pixels override optimistic AX echoes, closed controls are re-observed before selection input, and global menus are addressed only after confirming the intended frontmost app.
- 2026-07-29: plugin 0.7.0 closes the next exact-delivery gaps: pid-aware rehydration, activation-then-rehydrate, AXWindowNumber matching with ambiguity refusal, half-open Retina bounds, independently requested TCC grants, selected/focused AX handles that remain target-window-bound, shutdown cleanup for held buttons/screenshots, and process-local handle recovery guidance.
- 2026-07-28: plugin 0.8.0 isolates the signed Cua runtime under plugin data, pins and verifies its Cua AI Team ID and signing authority, persists and proves telemetry opt-out, disables update checks, atomically repairs unhealthy fallback runtimes, refreshes app process state on every listing, validates MCP arguments at the boundary, and tests the exact PyObjC closure across normal and free-threaded CPython 3.10-3.15.
- 2026-07-28: plugin 0.8.1 makes install, fallback, doctor, and the interactive live gate share one native-runtime probe and plugin-data environment; it also injects telemetry/update opt-outs into the LaunchServices-launched signed daemon instead of assuming shell environment inheritance.
- 2026-07-28: plugin 0.8.2 makes fresh final-state evidence an explicit Skill completion gate and corrects the fallback permission recovery wording; action responses alone are never treated as proof of a visible result.
- 2026-07-28: plugin 0.8.3 moves hosted macOS coverage to macOS 15 and runs the complete contract, native fallback, signed universal Cua install, and unrestricted policy proof on both Apple Silicon and Intel runners.
- 2026-07-28: plugin 0.8.4 completes fallback keyboard parity for common X keysym-style shifted punctuation (including `plus`, `colon`, and `ISO_Left_Tab`) and left/right Command aliases.
- 2026-07-28: plugin 0.8.5 makes raw mouse screenshot IDs fail closed: window IDs use window-relative pixel mapping, desktop IDs use their exact display mapping, and supplied IDs are never ignored.
- 2026-07-28: plugin 0.8.6 routes SIGTERM through MCP backend cleanup and tracks the latest delivered position for every held mouse button, so interrupted raw drags release at the real endpoint reached.
- 2026-07-28: plugin 0.8.7 enforces the exact five-package PyObjC 12.2.1 closure inside the backend itself, so a stale checkout `.venv` cannot bypass the pinned runtime contract; health output reports the resolved versions.
- 2026-07-28: plugin 0.8.8 fails every fallback synthetic-input and Accessibility action before dispatch when Accessibility TCC is absent, while preserving screenshot-only observation; macOS-silently-discarded input is never returned as success.
- 2026-07-28: plugin 0.8.9 preserves macOS front-to-back window enumeration while normalizing screenshot `zIndex` to the Codex convention that larger values are visually above smaller ones.
- 2026-07-28: plugin 0.8.10 elevates fallback screenshot binding into the main Skill: every image-derived window coordinate carries that exact fresh `screenshotId`, preventing Retina pixels from being misread as logical Quartz points.
- 2026-07-28: plugin 0.8.11 adopts Cua Driver 0.12.6's own `effect`, `verified`, `escalation`, and `degraded` verdicts from its reviewed macOS Skill: confirmed AX read-back remains subordinate to fresh completion evidence, unverifiable dispatch is treated as outcome-unknown, and suspected no-ops route directly to the next delivery rung without blind replay.
- 2026-07-28: plugin 0.8.12 follows the pinned macOS driver's native text paths: Electron/Catalyst can use atomic pixel-focus `type_text(x,y)`, while `type_text_incomplete` retries only the reported remaining suffix instead of duplicating already delivered characters.
- 2026-07-28: plugin 0.8.13 gives the direct Quartz fallback the same partial-text recovery contract: structured MCP errors preserve the exact delivered-character boundary, and any failed/partial input expires prior screenshot and AX handles before suffix-only recovery.
- 2026-07-28: plugin 0.8.14 makes failed delivery conservative across every direct input: old observations expire in `finally`, and keyboard/mouse down events are pre-registered so immediate retry or MCP shutdown can always post the matching release.
- 2026-07-28: plugin 0.8.15 incorporates three pinned macOS Skill recovery cases without its approval policy: missing screenshots never produce guessed coordinates, sparse Chromium AX gets one bounded refresh, and minimized-window key no-ops switch to fresh actionable AX controls.
- 2026-07-28: plugin 0.8.16 passes the official Codex plugin packaging shape audit: its separate `.agents` marketplace already carries required source/policy/category metadata, and `interface.defaultPrompt` is now a bounded string array instead of a non-canonical scalar.
- 2026-07-28: plugin 0.8.17 isolates Cua telemetry persistence under Plugin data through `CUA_DRIVER_TELEMETRY_HOME`; the fail-closed opt-out proof remains, while tests prove the user's unrelated `~/.cua-driver/config.json` is not created or modified.
- 2026-07-28: plugin 0.8.18 adds pip's `--no-cache-dir` to fallback first-use installation, keeping the hash-locked PyObjC closure inside the atomically published private venv without writing the user's shared pip cache.
- 2026-07-28: plugin 0.8.19 makes fallback screenshot publication fail-closed: the temporary directory must be a current-user-owned non-symlink at mode 0700, and capture timeout/write/error paths immediately remove unpublished partial PNGs and cache entries.
- 2026-07-28: plugin 0.8.20 moves the same ownership/non-symlink gate in front of orphan screenshot cleanup, so startup cannot follow a substituted temporary-directory link and delete unrelated old PNG files.
- 2026-07-28: plugin 0.8.21 bounds fallback Accessibility selection expansion to 64 extra rows/cells/children and reports `truncated:true`, closing the unbounded native-object cache path exposed by very large selections.
- 2026-07-28: plugin 0.8.22 verifies fallback clipboard writes by exact read-back and reports clear-then-write or read-back failures as structured partial effects, so a retry cannot silently erase or overwrite clipboard state twice.
- 2026-07-28: plugin 0.8.23 makes fallback observation assembly transactional: a failed AX channel rolls back its completed window PNG, and a failed display rolls back every earlier image from the same desktop call.
- 2026-07-28: plugin 0.8.24 aligns fallback action verdicts with pinned Cua semantics: dispatched Quartz/AX input is explicitly unverifiable, activation and pointer read-back can be confirmed, and `set_value` is confirmed only after an exact AX read-back.
- 2026-07-28: plugin 0.8.25 treats launch/activation as state-changing even when confirmation fails, invalidates prior observations, reports unknown launch outcomes structurally, and never turns a failed post-move cursor read-back into a replayable action failure.
- 2026-07-28: plugin 0.8.26 invalidates every fallback observation before native TCC prompts/System Settings and bounds retained Accessibility native objects to 32 observed windows with lower-watermark eviction.
- 2026-07-28: plugin 0.8.27 adds native input delivery truth: transient releases retry without replay, double release failures and interrupted drags are structured partial effects, unknown CGEvent posts stay unverifiable, and shutdown retains pending releases.
- 2026-07-28: plugin 0.8.28 hardens the fallback stdio server to JSON-RPC 2.0 semantics: notifications never receive responses, null/invalid initialize params get `-32602`, valid non-object messages get `-32600`, syntax failures stay `-32700`, and unexpected request failures use `-32603` without killing the process.
- 2026-07-28: plugin 0.8.29 upgrades the logged-in Mac live gate to require structured action verdicts on both backends, re-observe and prove fallback text-field focus before keyboard input, and force-kill an MCP child that ignores graceful close and terminate.
- 2026-07-28: plugin 0.8.30 resolves and expands fallback `.app` launch paths before calling macOS `open` and reuses that exact canonical path for running-process matching, fixing user-level `~/Applications` launches.
- 2026-07-28: plugin 0.8.31 prevents same-call double delivery: a failed/unknown advertised AX press or secondary action returns `suspected_noop` with a pixel recommendation, while coordinate fallback remains automatic only when the element never advertised AXPress.
- 2026-07-28: plugin 0.8.32 applies the one-rung invariant to `set_value`: focus/select/type is immediate only for an explicitly non-settable AX value, while failed writes to settable or unknown attributes return `suspected_noop` before any retyping.
- 2026-07-28: plugin 0.9.0 adds the pinned MIT Open Computer Use implementation at revision `a265277f6677ef00a1c597f54616cc3410d8d297` as a second macOS reference and adopts its non-safety AX lessons: best-effort Chromium visibility modes, merged window/menu/rows/contents/visible-child traversal, and adjustable 1,200-node/64-level fallback observations with explicit truncation reasons.
- 2026-07-28: plugin 0.9.1 adopts Open Computer Use's bounded-image lesson without weakening coordinate truth: fallback PNGs target a 1,280 px longest edge and 900 KB, atomically publish only a complete resized image, cache its actual pixel dimensions for Retina/display mapping, and retain the original if macOS `sips` cannot resize it.
- 2026-07-28: plugin 0.9.2 improves fallback AX decision quality with compact subroles, distinct descriptions/help/placeholders/identifiers, selected/expanded/disabled/settable and value-type traits, plus declared actions; default or duplicate fields stay omitted so large observations remain usable.
- 2026-07-28: plugin 0.9.3 brings fallback key chords closer to physical keyboard use: it posts real modifier down/up transitions, cleans already-posted modifiers on later creation failure, retains exhausted key releases for shutdown without same-call replay, and accepts Open Computer Use/X11 aliases for Spacebar, Del/Insert, Prior/Next, Caps_Lock, and keypad navigation.
- 2026-07-28: plugin 0.9.4 adopts Open Computer Use's exact-window activation fallbacks: after `AXRaise` refusal it tries `AXMain=true` then `AXFocused=true`, but still sends no input until frontmost pid and focused AXWindowNumber confirm the bound target.
- 2026-07-28: plugin 0.9.5 posts a real `MouseMoved` before every direct fallback click, drag start, and raw mouse-down, preserving hover-sensitive behavior and the physical move/down/up order while retaining exact release cleanup.
- 2026-07-28: plugin 0.9.6 makes raw held-button release physically coherent: a changed endpoint receives `MouseDragged` before `MouseUp`, unchanged endpoints do not invent motion, release is still attempted after an uncertain final drag, and twice-unconfirmed releases remain registered for shutdown cleanup. Cross-platform tests compare the runtime `kCGEventMouseMoved` constant because native macOS exposes integer `5`, not the fallback fixture label.
- 2026-07-28: the native macOS contract gate constructs real PyObjC keyboard, mouse-move/down/drag/up, click-state, and pixel-scroll events without posting them, catching bridge signature or constant regressions on both runner architectures without requiring TCC.
- 2026-07-28: plugin 0.9.7 corrects granular TCC readiness: Accessibility controls AX/input readiness, Screen Recording independently controls pixel/desktop observation readiness, and `fullComputerUseReady` requires both; screenshot-only capability is no longer hidden when Accessibility is absent.
- 2026-07-28: current ZCode Plugin docs revalidated the repository's `.zcode-plugin/plugin.json`, relative marketplace source, `.mcp.json`, `${CLAUDE_PLUGIN_ROOT}`/`${ZCODE_PLUGIN_ROOT}`, and `${CLAUDE_PLUGIN_DATA}` conventions; user instructions now use ZCode's `/` Skills picker instead of a Codex-style `$skill` prompt.
- 2026-07-28: plugin 0.9.8 routes fallback-only details into `references/tool-api.md`, shrinking the always-loaded Skill from about 20.2 KB to 16.8 KB while contract tests retain every critical targeting, freshness, verdict, and Full Access invariant. Its fallback venv is now keyed by the PyObjC version plus the exact requirements lock digest (`pyobjc-12.2.1-f76ce5003027`) rather than the Plugin version; tests recompute that digest, while Skill/docs releases reuse the tested runtime and avoid needless installs or Python TCC path churn.
- 2026-07-28: plugin 0.9.9 corrects physical modifier state on direct fallback chords: each Command/Control/Option/Shift key-up now removes its own Quartz flag while retaining only modifiers still held, and a modifier-only press reports a flagged down followed by an unflagged up. Event-level tests cover normal, construction-failure, and retained-release paths.
- 2026-07-28: plugin 0.9.10 marks direct fallback `KP_*` key-down and key-up events with Quartz's numeric-pad flag in addition to their keypad virtual keycodes, preserving the hardware-region signal used by native event handlers. The macOS contract constructs and reads back this real event flag without posting input.
- 2026-07-28: plugin 0.9.11 unifies window and desktop Quartz scroll delivery, replaces Python's half-to-even rounding with symmetric half-away-from-zero pixel quantization, returns the actual `deliveredDelta`, and rejects sub-half-pixel requests that would otherwise post `(0,0)` while reporting false success. Tests pin axis direction, location, zero-event refusal, and unknown-delivery verdicts.
- 2026-07-28: plugin 0.9.12 keeps the synthetic same-point hover move before every click but no longer writes double/triple-click state onto that `MouseMoved` event; only each physical down/up pair receives click states 1, 2, 3, matching native multi-click semantics.
- 2026-07-28: plugin 0.9.13 adopts Open Computer Use's non-policy input-pacing lesson: direct click move/down/up transitions now settle for 30 ms each, while timed pointer moves and drags wait before each interpolated frame so their final point lands at the requested duration rather than one frame early. Deterministic timeline tests cover all three paths.
- 2026-07-28: plugin 0.9.14 gives every completed fallback Unicode key pair a 20 ms app-event-loop settlement interval before sending the next chunk, borrowing Open Computer Use's reliability pacing without changing literal text, partial-delivery boundaries, or the unrestricted permission model.
- 2026-07-28: plugin 0.9.15 waits 100 ms after a fully released fallback key chord before returning, preventing the required fresh observation from racing native shortcut handlers; failed or pending releases skip this success-only settlement path.
- 2026-07-28: plugin 0.9.16 applies the same 30 ms fallback pointer pacing to drag start/release, raw mouse down/up, and final move read-back. Movement duration still measures only interpolated travel, while retained-button cleanup remains intact across interruptions.
- 2026-07-28: plugin 0.9.17 waits 100 ms after a successfully posted fallback pixel-scroll event before returning, matching the pinned Open Computer Use event-loop pacing so the mandatory fresh observation does not race an in-flight scroll; failed posts never enter the success delay.
- 2026-07-28: plugin 0.9.18 packs fallback Unicode input into at most 64 UTF-16 units per Quartz key pair, matching the pinned reference's event size while never splitting a Unicode code point. ASCII uses fewer synthetic events, astral characters retain exact unit lengths, and partial-delivery indexes remain Python-string suffix boundaries.
- 2026-07-28: plugin 0.9.19 lazily creates and reuses one Quartz `hidSystemState` source for every fallback mouse event, matching the pinned reference's physical input identity across move/down/drag/up. Native Apple Silicon and Intel tests construct mouse events from that real PyObjC source without posting them.
- 2026-07-28: plugin 0.9.20 extends the opt-in logged-in-Mac gate with a real screenshot-bound Quartz coordinate click against the disposable AppKit fixture, proves its visible result after re-observation, and restores the user's original cursor on success or best-effort failure cleanup.
- 2026-07-28: plugin 0.9.21 adds a native NSSlider to the disposable live fixture and drives it with a screenshot-bound Quartz drag, requiring a fresh AX/screenshot observation to prove the slider reached at least 80 percent before continuing. The same gate restores the original pointer afterward.
- 2026-07-28: plugin 0.9.22 adds a colored native scroll probe to the disposable live fixture. A screenshot-bound fallback pixel scroll must hit that exact view, increment its visible/AX counter, and survive a fresh screenshot/text observation before the live gate restores the cursor and exits.
- 2026-07-28: plugin 0.9.23 extends the logged-in-Mac gate with the raw fallback pointer family: screenshot-bound `mouse_down` grabs the slider at its high end, held `move_mouse` drags it back, `mouse_up` releases it, and a fresh observation must prove a value at or below 20 percent before continuing.
- 2026-07-28: plugin 0.9.24 makes fallback shutdown a stronger final stuck-input recovery point: every retained mouse-up and key-up gets up to three bounded attempts with 10 ms gaps. Repeated releases are idempotent, and no matching down event is ever replayed.
- 2026-07-28: macOS CI now launches the verified Cua 0.12.6 binary on a disposable socket with the permissions onboarding disabled only for this non-GUI proof, checks its real status for unrestricted mode plus absent user/managed/session policies, then stops and reaps it.
- 2026-07-29: plugin 0.10.0 upgrades the pinned primary backend to Cua Driver 0.13.1 at exact release commit `d8c1efac808333bbecfcb2a9ff6705b5b1e6195a` and archive SHA-256 `236fc1aa02a09046945074623a02c86646b0be4c48754c6f502f9b1fff2bc032`. Source and a real 0.13.1 daemon prove that unrestricted mode still directly allows observation, desktop input, file transfer, browser consequential actions/scripts, and process control while user/managed/session policies remain absent. Public `check_permissions(prompt=true)` is now intentionally refused as a macOS TCC boundary, so the Skill uses read-only checks and a human-run signed LaunchServices `permissions grant` helper; auto-session escalation, snapshot-bound `element_token`, and browser field replacement guidance were adopted from the current upstream macOS Skill without importing its action-policy restrictions.
- 2026-07-29: plugin 0.10.1 adopts Cua Driver 0.13.1's per-session colored semantic cursor as the human-visible primary interaction layer. Declared sessions keep it enabled by default; background observation, click, drag, scroll, text, key, navigation, app, and system actions animate it without moving the user's real pointer. The Skill documents explicit per-session hide/read-back and cleanup, the launcher requires the cursor tools, and the logged-in Mac gate toggles the exact session cursor, drives snapshot-bound `element_token` actions when advertised, then proves that real actions update the cursor position.
- 2026-07-29: plugin 0.10.2 adopts the current macOS Skill's virtual-cursor visibility distinction without its action-policy restrictions. `move_cursor(scope:"window")` seeds or glides only the per-session overlay in screen-point space and delivers no input; `scope:"desktop"` remains a real-pointer operation grounded in a fresh desktop screenshot. The signed-driver launcher requires both cursor-position/screen-size inspection and movement tools, while the logged-in Mac gate moves the overlay to a deterministic point and proves the user's real pointer did not move before continuing the normal action loop.
- 2026-07-29: plugin 0.10.3 makes concurrent work human-identifiable through Cua Driver's local cursor badge and stable per-label color. The Skill now requires a concise task slug plus short uniqueness suffix, bounded to the upstream badge's 28 visible characters and free of secrets/copied user content; the live Mac gate uses and reports the same bounded label instead of an unreadable full UUID.
- 2026-07-29: plugin 0.10.4 promotes primary `double_click` and `right_click` from incidental upstream tools to mandatory signed-driver capabilities. The disposable AppKit fixture now exposes a visual gesture probe, and the logged-in Mac gate grounds each gesture in a fresh window PNG and proves its distinct visible result before moving on.
- 2026-07-29: plugin 0.10.5 extends the signed-primary live gate through continuous pointer input: a 350 ms/16-step foreground drag must move the visible AppKit slider near its end, and a separately re-grounded background wheel event must change the scroll probe. The Skill records the pinned driver's explicit `background_unavailable` window-drag boundary and permits one fresh foreground retry as a technical delivery rung, not an approval prompt.
- 2026-07-29: plugin 0.10.6 closes the primary keyboard live-test gap. The focused AppKit field handles a real Command-Shift-K event with visible readback and the submit button owns a Space key equivalent; the live gate separately proves pid/window-bound background `hotkey`, snapshot-bound `press_key`, literal `type_text`, and mouse `click`, refreshing state between every input. A field responder is intentional because native menu key equivalents may require the driver's foreground delivery rung.
- 2026-07-29: plugin 0.10.7 adds independent non-text AX control proof to the signed-primary gate. After a pixel drag, a fresh slider `element_token` is passed to `set_value(25)`; the call must return `effect:"confirmed"`/`verified:true`, and a subsequent snapshot must expose a numeric value within 0.5 of 25. This also proves token refresh discipline rather than conflating drag output with AX value delivery.
- 2026-07-29: plugin 0.10.8 closes the signed-primary desktop-scope proof and corrects the 0.13.1 escalation request shape: `reason` is required and optional detail must be bounded/non-sensitive. The live session now starts `auto`, escalates with `no_window_target`, reads back effective desktop state, grounds a real-pointer nudge in a fresh primary-display PNG, restores the pre-test pointer (including a Quartz cleanup path for another display or errors), and takes a final desktop screenshot. This also fixes restoration after the foreground drag, which necessarily moves the real pointer.
- 2026-07-29: plugin 0.10.9 proves isolated app lifecycle control without touching a user's existing instance. The live gate records running Calculator pids, calls `launch_app` with bundle ID plus `creates_new_application_instance:true`, requires a new pid and a window owned by it, then sends pid/window-bound foreground Command-Q and verifies process exit. `kill_app` is now a mandatory unrestricted capability but is used only as bounded cleanup for that exact still-live disposable pid after cooperative quit fails; cleanup repeats best-effort on any test error.
- 2026-07-29: plugin 0.11.0 promotes the complete Cua Driver 0.13.1 typed browser family to the mandatory signed-primary surface: setup, exact native-window binding, semantic snapshots, navigation, click/type/pointer actions, page dialogs, uploads, and downloads. The Skill now requires an exact mutable binding, snapshot-scoped advertised capabilities, PNG-to-CSS scaling, fresh refs after every mutation, and an explicit trusted-to-DOM-event choice instead of a silent fallback. The plugin adds no browser allowlist or approval prompt; it preserves the dependency's endpoint/window ownership and download-host proof, uses native UI when those technical proofs are unavailable, and never fabricates private approval fields.
- 2026-07-29: plugin 0.11.1 turns the typed browser request shape into a signed-binary CI contract rather than trusting names or documentation. A local verifier runs `cua-driver describe` for all nine tools and checks the exact pinned version plus critical properties, required fields, enum values, defaults, upload bounds, and download destination root. Both hosted macOS architectures execute it against the Team-ID-verified universal app before any daemon starts, so an upstream release/schema mismatch fails closed without touching a browser profile.
- 2026-07-29: plugin 0.11.2 adds Cua Driver's session-scoped motion physics to the mandatory human-visible cursor contract. The Skill distinguishes Bezier/arc/spring/timing presentation from input delivery and forbids random target jitter; temporary tuning must be read back and restored. The logged-in-Mac gate snapshots the current motion, applies and independently reads back a speed-based human-readable preset, exercises the existing action loop with it, restores every supported numeric field, verifies the restored state, and still ends the session if best-effort restoration fails during error cleanup.
- 2026-07-29: plugin 0.11.3 adopts the signed primary `zoom` crop and `from_zoom` coordinate path for small visual targets. The Skill treats the zoom registry as one replaceable context per pid, permits one immediate action only on the same returned pid/window, and forbids mixing full-window pixels or carrying the context across another window, zoom, layout/process, or MCP change. The logged-in-Mac gate crops a symmetric fresh region around the disposable gesture probe, requires a valid JPEG image block, clicks its center in zoom space, then refreshes the full window and requires the distinct `Gesture: left` readback.
- 2026-07-29: plugin 0.11.4 makes the pinned macOS `bring_to_front({pid,window_id})` surface mandatory for explicit persistent foreground and focus-proxy workflows. It is not inserted into the normal input ladder: one-off foreground delivery still fronts, acts, and restores. The Skill pins the no-session schema, requires an exact returned window whenever one exists, and mandates fresh `list_apps.active`, exact re-list, and window re-observation after activation. The logged-in-Mac gate exercises the signed WindowServer/Cocoa path against the disposable AppKit fixture and proves the same pid/window is frontmost before continuing.
- 2026-07-29: plugin 0.11.5 makes Cua Driver's stable schema-v1 `health_report` a mandatory read-only preflight before the exact permission check. The Skill consumes structured named checks, tolerates additive names, distinguishes core failure from degraded TCC/capability diagnostics, and never treats a hint as an approval layer. The logged-in-Mac gate requires the pinned version, darwin platform, passing binary/session/bundle/TCC/AX checks, and the deliberate read-only ScreenCaptureKit skip before exercising the UI.
- 2026-07-29: plugin 0.11.6 makes `set_agent_cursor_theme` mandatory and pins its session-scoped `{theme_id,reduced_motion}` use. Agent calls may select only locally installed theme IDs and cannot inject paths, URLs, source art, or inline animation; custom authoring/install stays a trusted local CLI workflow rather than an approval layer. The logged-in-Mac gate keeps the current installed theme ID, changes and independently reads back reduced-motion mode, then restores and verifies both on success or best-effort cleanup.
- 2026-07-29: plugin 0.11.7 makes the four local trajectory tools mandatory and routes their detailed contract to an on-demand recording reference. Recording remains unrestricted but explicit because it writes screenshots, UI state, and literal action arguments locally. The Skill checks the daemon-global recorder is idle, binds one exact output directory, defaults video off, refuses to clobber or unconditionally stop another owner, and restricts replay to an inspected same-live-window pixel/keyboard trajectory with fresh final verification. The logged-in-Mac gate records one disposable exact-window right click, verifies ordered JSON and before/after PNG evidence, stops it, and removes only that test directory.
- 2026-07-29: plugin 0.11.8 pins the primary macOS `click.action` semantics for `press`, `show_menu`, `pick`, `confirm`, `cancel`, and `open`, and adds the frontmost native menu two-snapshot pattern. Closed menu children are never guessed: the Skill picks a fresh `AXMenuBarItem`, refreshes, presses the newly returned `AXMenuItem`, and refreshes again. The logged-in-Mac fixture now owns a real AppKit menu whose command changes an AX-visible label, proving exact foreground activation plus semantic menu open/selection end to end.
- 2026-07-29: plugin 0.11.9 adopts the pinned background `launch_app.urls` path for opening native files, folders, and resource URLs without shell `open` or AppleScript activation. Local paths and `file://` targets are preflighted with structured `FILE_NOT_FOUND`; returned pid/windows and `self_activation_suppressed` are routing evidence, not visible completion proof. The logged-in-Mac gate creates one unique plugin-data folder, opens it in Finder while requiring the AppKit fixture to remain frontmost, observes the exact returned Finder window, closes only that window with pid/window-bound Command-W, verifies disappearance, and removes only the temporary folder.
- 2026-07-29: plugin 0.12.0 exposes the pinned driver's session-effective `get_config`/`set_config` image-fidelity contract without making global configuration part of normal automation. Named sessions may temporarily set `max_image_dimension:0` for explicit pixel-perfect verification, must read back and restore the prior value or end the session, and must never use the retired capture-mode/scope keys. The logged-in-Mac gate verifies the exact driver version and rejects a conflicting source SHA when the build advertises one, changes the current session to a distinct image cap, proves a fresh peer session still sees the original value, and restores/read-backs the original before GUI control.
- 2026-07-29: plugin 0.12.1 makes `get_accessibility_tree` part of the pinned primary contract as a lightweight content-free desktop inventory. It is discovery only, emits apps plus visible pid/window candidates without actionable element handles, and never replaces exact `get_window_state` grounding. The logged-in-Mac gate finds the unique AppKit fixture through this inventory before the explicit permission-status call, then requires later exact `list_windows` enumeration to return the same window ID.
- 2026-07-29: plugin 0.12.2 closes the primary reuse gap by requiring `get_config`, `set_config`, and `get_accessibility_tree` from the signed pinned binary before an installed Cua Driver app is accepted. The image-fidelity note now distinguishes an anonymous CLI/direct call, which can persist global configuration, from the public named session used by normal MCP automation.
- 2026-07-29: plugin 0.13.0 corrects the image-fidelity boundary to the pinned binary's advertised request shape: `get_config` and `set_config` have no public `session` field and resolve through the daemon-injected MCP-connection identity. The live gate changes one connection, starts a second real primary proxy against the same daemon and proves it retains the original value, then restores the first connection. A new macOS CI verifier pins the exact closed request schemas for health, permissions, discovery, configuration, background launch, and persistent exact-window activation directly from the signed release binary's `describe` output.
- 2026-07-29: plugin 0.13.1 extends signed-binary schema verification to nineteen native lifecycle/configuration contracts, covering session scope and bounded escalation, exact cursor controls, and local recording/replay in addition to the earlier health/config/app tools. It adopts the pinned `start_session.cursor_theme` nested shape so an installed theme and reduced-motion mode are applied atomically before first cursor visibility; the logged-in-Mac gate starts with `cua.default` plus `auto` and requires exact readback before later temporary theme tests.
- 2026-07-29: plugin 0.13.2 adopts the current upstream browser cursor feedback contract. Ref/coordinate page mutations reuse the declared session's semantic overlay without moving the physical pointer or changing focus/z-order; only a safely mapped selected tab may display it, unprovable child-frame positions are omitted, and navigation invents no target motion. The overlay remains presentation, never delivery or completion evidence, so every mutation still ends with a fresh page snapshot.
- 2026-07-29: plugin 0.13.3 expands signed-binary schema verification from nineteen to thirty-four native contracts. Exact observation, desktop/window cursor, click/double/right-click, drag, scroll, text/key/hotkey, AX value, and zoom request shapes are now checked before any TCC-dependent live action, so an upstream action-surface drift fails the macOS CI gate instead of first appearing on a granted interactive Mac.
- 2026-07-29: plugin 0.14.0 pins `list_apps`, `list_windows`, and `kill_app`, completing exact signed-binary request-schema coverage for all 46 tools that the primary launcher requires (thirty-seven native plus nine typed browser). Startup still separately rejects a binary missing any tool, while CI now also rejects any request-shape drift across the whole mandatory primary surface.
- 2026-07-29: plugin 0.14.1 makes complete primary coverage a machine-checked invariant: the startup launcher's required-tool set must exactly equal the disjoint union of native and typed-browser schema contracts and remain 46 tools. Adding, removing, duplicating, or forgetting a schema now fails the ordinary repository contract suite before macOS CI.
- 2026-07-29: plugin 0.14.2 adds a no-TCC live daemon gate on both hosted Mac architectures. The verified unrestricted service must return structured app/window inventory plus positive screen geometry and numeric cursor position, then successfully `kill_app` only a disposable CI-owned sleep process and prove that exact pid is gone. This tests real local discovery and unrestricted process-control execution rather than schema presence alone.
- 2026-07-29: plugin 0.15.0 adds a signed-primary stdio MCP end-to-end gate on both Mac architectures. It negotiates MCP, requires all 46 pinned tools in `tools/list`, executes permission-free app/window/screen/cursor discovery through `tools/call`, then asks unrestricted `kill_app` to terminate a verifier-owned sleep process and requires the child to exit by SIGKILL. The disposable process cleanup is owned by the verifier, so failures do not leak it.
- 2026-07-29: plugin 0.15.1 compares every one of the 46 mandatory schemas advertised to ZCode by MCP `tools/list` against the exact `input_schema` returned by direct `describe` from the same signed binary. The CI gate now rejects a stdio-proxy schema translation or registration drift even when the underlying driver contract remains correct.
- 2026-07-29: plugin 0.15.2 extends the no-TCC signed-primary MCP gate through session lifecycle. It creates a unique strict `window` session, requires active window-effective state from both `start_session` and `get_session_state`, explicitly ends it with `active:false`, and best-effort ends it during verifier cleanup on any later failure. Session identity/capture state is therefore exercised independently of the logged-in TCC GUI gate.
- 2026-07-29: plugin 0.16.0 closes the signed-app reuse identity gap. In addition to the archive hash, Gatekeeper, Team ID, signing authority, version, and complete tool surface, every installed-app reuse now requires the main `cua-driver` executable and `cua-cursor-theme` helper to match their exact 0.13.1 release SHA-256 values. A different validly signed same-version bundle is rejected and replaced from the pinned archive rather than being mistaken for the audited upstream artifact.
- 2026-07-29: plugin 0.16.1 extends the signed-primary stdio MCP gate with three permission-free ownership invariants on both Mac architectures: a `set_config` image cap is visible on the same connection but not a live peer and is restored; a started session's semantic cursor can be disabled, read back, and re-enabled; and an `auto` session remains window-effective until explicit `no_window_target` escalation, then becomes irreversibly desktop-effective until it is ended. These paths now execute through the same proxy identity injection ZCode uses without depending on interactive TCC.
- 2026-07-29: plugin 0.16.2 moves the primary preflight itself into the signed stdio gate. The CI daemon is launched through the signed app with LaunchServices exactly like the Plugin, not by directly executing the nested binary (which correctly attributes TCC state to the caller). A real ZCode-shaped connection must receive schema-v1 macOS health with all four core/bundle checks passing, a deliberately skipped direct-capture probe, and TCC booleans consistent with a separate read-only `check_permissions(prompt=false)` attributed to the `com.trycua.driver` daemon. The public `prompt=true` shape must return `os_permission_prompt_requires_trusted_host` through a disposable MCP peer without opening permission UI; the refusal is tested last because Intel may close that proxy afterward. This preserves macOS TCC as the sole non-bypassable authorization boundary rather than introducing a plugin approval layer.
- 2026-07-29: plugin 0.16.3 moves native app lifecycle execution into the permission-free signed-primary stdio gate. On each hosted Mac architecture it selects an installed Calculator or TextEdit only when that bundle has no running pid, cold-launches it, accepts only a globally new positive pid, rebinds identity through a fresh app inventory or that pid's exact system app executable path, requires a returned window owned by the same pid, then invokes unrestricted `kill_app` on that exact disposable process and proves both the OS process and `list_apps` entry disappear. This avoids the nondeterministic multi-instance behavior of system apps, and failure cleanup never targets a pre-existing pid.
- 2026-07-29: plugin 0.17.0 closes the legacy browser mutation and Full Access gaps in the signed primary. The 47 Computer Use action/lifecycle tools are mandatory and schema-pinned, including the legacy `page` compatibility surface in addition to the nine typed browser tools. The plugin-private daemon starts with `CUA_DRIVER_ENABLE_LEGACY_PAGE_MUTATIONS=1`; daemon reuse and both hosted Mac architectures must prove an invalid JavaScript action reaches ordinary missing-pid validation rather than the dependency's default-disable guard. New browser work still uses typed exact target/tab/ref binding first, while legacy JavaScript, DOM click, insert-text, and keystroke routes remain available without a second approval layer and require fresh pid/window grounding plus post-action observation.
- 2026-07-29: plugin 0.17.1 makes the signed primary MCP inventory an exact 49-tool contract. The real stdio gate exposed two previously uncounted driver-service tools, `check_for_update` and `install_ffmpeg`, so both now have exact schemas and are mandatory alongside the 47 Computer Use tools. The gate rejects malformed entries, duplicate names, missing audited tools, and unexpected additions before comparing every advertised input schema with the pinned binary's direct description. Dependency drift therefore cannot silently widen or shrink the ZCode-visible surface.
- 2026-07-29: plugin 0.17.2 extends exact signed-primary parity beyond names and request schemas. Both hosted Mac architectures must match every stdio tool description and three stable MCP annotations against the same binary's machine-readable docs, preserve boolean open-world hints, unique string capabilities, the complete risk-metadata shape, and top-level capability/schema version `1`. The two driver-service tools additionally pin their exact read-only/destructive/idempotent/open-world annotations.
- 2026-07-29: plugin 0.17.3 pins the complete macOS MCP initialize result exposed to ZCode. The signed stdio proxy must negotiate protocol `2025-06-18`, advertise only the tools capability, identify itself exactly as `cua-driver` `0.13.1`, and return the audited macOS-specific Cua Driver workflow instructions byte-for-byte before the initialized notification and tool discovery proceed.
- 2026-07-29: plugin 0.17.4 disables Cua Driver's own first-launch permission gate on both the product and hosted-macOS LaunchServices paths by combining its supported `--no-permissions-gate` flag with `CUA_DRIVER_RS_PERMISSIONS_GATE=0`. The gate runs after socket binding and can restart a slow headless service while ZCode is already connected; removing that onboarding race stabilizes the unrestricted MCP daemon without granting or bypassing Accessibility or Screen Recording. TCC remains a one-time human-owned grant through the explicit installer route.
- 2026-07-29: plugin 0.17.5 pins the signed primary's JSON-RPC failure and notification behavior on both macOS architectures. After initialization the same live stdio connection must return exact `-32700 Parse error`, `-32601 Unknown method: ...`, and `-32602 Invalid params: missing tool name` envelopes, emit no response for initialized or unknown notifications, and continue into the complete audited tools list. Malformed or unsupported client traffic therefore cannot silently change the ZCode-facing protocol or kill a healthy unrestricted session.
- 2026-07-29: plugin 0.17.6 turns hosted-macOS TCC availability into observed state instead of an assumption. The signed primary verifier exports an exact daemon-attributed Accessibility/Screen Recording report; each architecture conditionally runs the existing disposable full GUI smoke only when both grants are already present, and otherwise records the missing-grant gate without prompting or modifying TCC. The plugin-owned live daemon is stopped on the exact private socket after the conditional run.
- 2026-07-29: plugin 0.17.7 splits the disposable live gate into `all`, `primary`, and `fallback` modes. Both hosted Mac architectures now unconditionally exercise the repository-owned direct backend against a real AppKit window after its native readiness check, including physical focus, desktop shortcut/text input, screenshot verification, timed slider drag, raw held-button movement/release, coordinate click, wheel input, and pointer restoration. The signed primary mode remains conditional on its separately attributed TCC report, so this adds real GUI evidence without pretending the Cua app has grants it lacks.
- 2026-07-29: plugin 0.17.8 fixes the first real hosted-GUI failure exposed by 0.17.7 on both architectures. AppKit accepted activation but the subprocess-hosted fixture did not become frontmost within 600 ms, so the fallback correctly sent no input. Foreground activation now also sets the exact pid's application-level `AXFrontmost` attribute, retains the existing exact-window `AXRaise`/Main/Focused ladder, and allows two seconds for asynchronous confirmation before still failing closed.
- 2026-07-29: plugin 0.17.9 adopts the exact-window foreground path found in the pinned Cua Driver source after 0.17.8 proved that public AppKit and `AXFrontmost` requests could both be accepted on hosted macOS 15 without changing WindowServer ownership. The fallback now resolves SkyLight locally, maps the fresh CGWindowID to its owning connection/PSN, and calls `SLPSSetFrontProcessWithOptions` for that exact pid/window before using public AppKit as a compatibility fallback. This is not a TCC bypass: Accessibility remains mandatory, and input is still withheld until the fresh frontmost pid and focused window ID both confirm the target.
- 2026-07-29: plugin 0.17.10 makes exact-window activation confirmation use the same authority as delivery. A process-backed AppKit fixture may be a real WindowServer foreground owner without being a normal LaunchServices application, so the fallback now reads `_SLPSGetFrontProcess` and compares its eight-byte PSN with the PSN resolved from the fresh target CGWindowID; the public `NSWorkspace` pid remains an independent compatibility confirmation. The exact AX focused window must still match before input, and a failed confirmation now returns bounded structured diagnostics covering both authorities without sending input.
- 2026-07-29: plugin 0.17.11 fixes the remaining activation readback mismatch proven identically on both hosted macOS architectures: WindowServer PSN, public frontmost pid, and `AXFrontmost` all matched the target, but the fixture intentionally exposed no `AXWindowNumber`, and separate PyObjC wrappers for the same AX window did not compare equal. Focus confirmation now prefers AXWindowNumber/object/CF equality, then compares the focused window with the target AX window's title, position, and size signature. `_ax_window` already rejects equal-distance binding ties, so the signature fallback stays exact rather than weakening the window scope.
- 2026-07-29: plugin 0.17.12 completes AX focus identity for sparse process-backed windows after 0.17.11 showed that the fixture's focused proxy also omitted a complete frame signature. CoreFoundation `CFEqual` is now loaded explicitly, and a final exact fallback accepts a non-null `AXFocusedWindow` only when `_ax_window` has already bound the fresh target CGWindowID to that pid's sole exposed AX window. Failed confirmation reports both focused/target signatures and exposed-window count; input remains withheld on any multi-window ambiguity.
- 2026-07-29: plugin 0.17.13 corrects the hosted fallback GUI gate after 0.17.12 passed exact foreground activation and reached the next step: the disposable AppKit fixture is a bare Python process, so macOS exposes its window/menu only as sparse `AXUnknown` nodes even though screenshots and input are ready. The fallback live test now exercises its text field, button, slider, raw held-button drag, and scroll probe entirely from fresh screenshot-bound coordinates, captures again after state changes, and verifies the fixture's atomically published local control state. The test still requests one AX observation to prove the channel is callable, but no longer mistakes packaging-specific AX sparsity for an input failure.
- 2026-07-29: plugin 0.17.13 is verified end to end by GitHub Actions run `30439665326` on both hosted macOS architectures. ARM64 job `90535665705` and Intel job `90535665628` each completed the real fallback AppKit GUI loop: exact foreground confirmation, screenshot-grounded click/text/shortcut, local submitted-state proof, post-action pixel change, timed and raw slider drags, scroll, pointer restoration, and cleanup. The signed primary GUI branch correctly remained gated because both hosted runners reported the signed `CuaDriver.app` TCC grants as false.
- 2026-07-29: plugin 0.17.14 makes the exact 49-tool primary surface agent-discoverable as well as schema-complete. The repository gate derives the native/browser contract union and requires every exact tool name to occur in the Skill documentation. It closes the two found omissions: `get_screen_size` is diagnostic geometry and never replaces fresh desktop/window pixels, while empty-input `check_for_update` is an explicit read-only open-world release advisory, never an automatic task preflight or updater. Ordinary automation keeps periodic update checks disabled, sends no captured GUI content to this advisory, and preserves the separately audited pinned runtime.
- 2026-07-29: plugin 0.17.14 is verified by GitHub Actions run `30440382181`. ARM64 job `90538023427` and Intel job `90538023466` both passed the full signed-binary, exact stdio contract, fallback first-run, and disposable real-GUI path; Windows and every supported free-threaded/non-free-threaded fallback Python matrix job also passed.
- 2026-07-29: plugin 0.17.15 corrects an agent-workflow shape mismatch found by comparing the pinned upstream macOS Skill semantics rather than tool names alone. Primary `get_window_state` always supplies AX `tree_markdown` and pixels together unless `include_screenshot:false`; it has no fallback-style `include_text`, and accepted `capture_mode` values are compatibility no-ops. The Skill now distinguishes the two backend schemas, documents exact primary response geometry and local `screenshot_out_file` transport, routes the structured macOS browser refusal codes, and describes every pinned per-turn trajectory evidence file. Repository tests lock these agent-facing semantics so exact schema parity cannot coexist with an invalid usage recipe.
- 2026-07-29: plugin 0.17.16 expands the permission-free signed-primary stdio gate from request-shape parity into exact live response parity for `get_accessibility_tree` and the no-video recorder lifecycle. Both macOS architectures must return the pinned app/window inventory fields, canonical eight-field recorder states, daemon-global peer readback, and a valid local schema-v1 `session.json` across start/state/stop without relying on TCC. An ARM CI failure also exposed that a killed AppKit process can leave a briefly stale NSWorkspace app record; the owned-app gate now waits separately for OS process exit and eventual `list_apps` disappearance instead of treating one stale read as a product failure.
- 2026-07-29: plugin 0.17.16 is verified by GitHub Actions run `30441754271`. ARM64 job `90542470435` and Intel job `90542470515` both passed the exact signed-primary AX inventory, daemon-global no-video recorder state/artifact lifecycle, eventual app-exit inventory, fallback real-GUI path, and all prior contract gates; Windows and every supported fallback CPython job also passed.
- 2026-07-29: plugin 0.17.17 pins the remaining permission-free identity records that an agent uses to select a real target. The signed-primary stdio gate now validates every nine-field `list_apps` entry, every ten-field layer-0 `list_windows` entry and bounds/Space type, the exact `launch_app` app and six-field window projection, fresh launch-window identity continuity, integer cursor coordinates, and logical display geometry. The Skill now makes the upstream distinction explicit: `list_apps` includes stopped apps as pid 0 and deliberately emits empty per-app `windows`; only a positive returned pid/window from `launch_app` or `list_windows` is actionable.
- 2026-07-29: plugin 0.17.17 is verified by GitHub Actions run `30442325234`. ARM64 job `90544303416` and Intel job `90544303365` both passed the exact signed-primary app/window/launch/screen/cursor identity responses, fallback real-GUI loop, and all preceding contracts; Windows and every supported fallback CPython job also passed.
- 2026-07-29: plugin 0.17.18 pins permission-free signed-primary outcome semantics, including failure paths. Unknown app and missing local-file launches must remain ordinary MCP tool errors with exact `APP_NOT_INSTALLED`/`FILE_NOT_FOUND` structured content and no lifecycle mutation; successful `kill_app` must remain a text-only exact-pid acknowledgement followed by observed process disappearance. Session start/state/escalate/end now require complete exact records, including pre-escalation null reason/detail and `revived:false` for the fresh CI-owned session. The Skill tells ZCode to repair a bad target rather than misclassifying these outcomes as transport loss, TCC denial, or another approval boundary.
- 2026-07-29: plugin 0.17.18 is verified by GitHub Actions run `30443007423`. ARM64 job `90546515220` and Intel job `90546515244` both passed exact signed-primary structured launch errors with no lifecycle mutation, text-only exact-pid kill acknowledgement, complete session-state records, fallback real-GUI execution, and all preceding contracts; Windows and every supported fallback CPython job also passed.
- 2026-07-29: plugin 0.17.19 pins the permission-free signed-primary semantic cursor state that makes background operation human-visible without moving the real pointer. The gate requires the complete six-field state, embedded `cua.default` theme identity, all nine exact human-like motion defaults, bounded dynamic visual metadata, exact theme/motion setter responses, disable readback, re-enable, and restored readback for a fresh public session. Both setters are exercised idempotently, so the gate proves the public contract without leaving modified visual settings or depending on TCC. Initial CI facts on both architectures showed that a read can legitimately observe the preceding control as `system` + `desktop` + `one_shot`; validation therefore pins the action/modifier/phase domains and numeric counters without imposing a false idle-timing requirement.
- 2026-07-29: plugin 0.17.19 is verified by GitHub Actions run `30443830048`. ARM64 job `90549220052` and Intel job `90549220076` both passed the exact signed-primary semantic-cursor state, dynamic visual domains, idempotent setters and visibility restoration, fallback real-GUI path, and every preceding contract; Windows and every supported fallback CPython job also passed.
- 2026-07-29: plugin 0.17.20 expands signed-primary health parity from top-level identity and named-status checks to the complete schema-v1 response contract. The real driver gate now pins all eight macOS checks in canonical order, required and optional entry fields, fail-only non-empty remediation hints, the closed typed diagnostic-data field set, exact signed bundle/version/platform/session semantics, TCC-to-AX consistency, read-only ScreenCaptureKit skip, and status-derived `overall`. A deterministic `include:["binary_version"]` call proves that filtering still returns the complete ordered map with seven exact skips and does not run TCC probes; agents continue to consume structured content rather than decorative text.
- 2026-07-29: plugin 0.17.20 is verified by GitHub Actions run `30444552769`. ARM64 job `90551609806` and Intel job `90551609803` both passed the complete signed-primary schema-v1 health entries, filtered ordered-skip map, TCC/AX consistency, fallback real-GUI path, and all preceding contracts; Windows and every supported fallback CPython job also passed.
- 2026-07-29: plugin 0.17.21 pins the complete signed-primary configuration response and isolation contract. The release `get_config` must return exactly seven fields with version 0.13.1, null maintainer-only source SHA, macOS platform, non-negative effective image limit, exact enabled cursor object, disabled experimental PiP, and null PiP geometry in the isolated CI home. Direct-field and `{key,value}` image-limit setters must return the exact three-field result plus session-scoped text, affect only their injected MCP-connection identity, preserve a concurrent peer, restore the original snapshot, and create no persisted config file. The retired `capture_scope` key must remain an exact structured tool error pointing to `start_session.capture_scope` and leave the peer snapshot unchanged.
- 2026-07-29: plugin 0.17.21 is verified by GitHub Actions run `30445124333`. ARM64 job `90553482645` and Intel job `90553482594` both passed the exact signed-primary configuration reads, both setter shapes and text, retired-key error, peer isolation, restoration, isolated-HOME no-persistence assertion, fallback real-GUI path, and all preceding contracts; Windows and every supported fallback CPython job also passed.
- 2026-07-29: plugin 0.17.22 pins the complete signed-primary TCC preflight response rather than only its top-level booleans. Empty input and explicit `prompt:false` must return the same five-field structured state and exact text derived from the two booleans plus the deliberate no-direct-capture-probe note. The seven-field source record must identify a live process distinct from both MCP proxies and the verifier, use the signed app executable path and `com.trycua.driver` bundle, preserve responsible-parent/disclaim types, and carry the exact daemon-attribution note. Health TCC statuses must agree. Public `prompt:true` remains the sole macOS-owned authorization boundary and is now pinned as an exact `status:refused` tool-error payload/text with `os_permission_prompt_requires_trusted_host`, never a plugin approval or bypassable agent action.
- 2026-07-29: plugin 0.17.22 is verified by GitHub Actions run `30445788068`. ARM64 job `90555686625` and Intel job `90555686630` both passed exact signed-primary read-only/default permission payloads and Unicode summaries, live daemon source identity, health agreement, full trusted-host prompt refusal, fallback real-GUI execution, and every preceding contract; Windows and every supported fallback CPython job also passed.
- 2026-07-29: plugin 0.17.23 pins the complete no-TCC `get_accessibility_tree` quick-inventory presentation contract from the signed primary instead of treating its field names alone as parity. Both independent MCP connections must return exact app/window collections with positive unique process/window identities, non-empty regular-app/WindowServer owner names, nullable-but-non-empty bundle IDs, and titles as strings. The exact Unicode text summary is reconstructed from the same ordered structured arrays, including the empty-title and window-inspection guidance forms, so an agent cannot mistake decorative prose for a second observation source. This remains AppKit/CGWindow discovery and never claims or attempts a TCC bypass; exact target grounding still requires fresh `list_windows`/`get_window_state`.
- 2026-07-29: plugin 0.17.23 is verified by GitHub Actions run `30446409566`. ARM64 job `90557764144` and Intel job `90557764066` both passed the signed-primary dual-connection no-TCC app/window inventory, positive unique identity checks, exact structured-to-Unicode-summary reconstruction, fallback real-GUI execution, and every preceding contract; Windows and every supported fallback CPython job also passed.
- 2026-07-29: plugin 0.17.24 pins the remaining permission-free passive-discovery presentation responses from the signed primary. The full `list_apps` record now drives an exact Unicode total/running/stopped summary plus ordered running-app lines; `list_windows` drives its exact count text; `get_screen_size` requires positive integer logical main-display dimensions, a positive half-step floating backing scale, and exact point/scale text; `get_cursor_position` requires integer screen-point coordinates, including valid negative multi-display positions, and exact text. Structured values remain the only target authority, and neither global geometry nor cursor position replaces fresh desktop/window screenshot grounding.
- 2026-07-29: plugin 0.17.24 is verified by GitHub Actions run `30446975186`. ARM64 job `90559633599` and Intel job `90559633577` both passed exact signed-primary app/window summaries, floating half-step main-display geometry and text, integer cursor coordinates and text, fallback real-GUI execution, and every preceding contract; Windows and every supported fallback CPython job also passed.
- 2026-07-29: plugin 0.17.25 corrects and pins the signed-primary daemon-global recorder ownership contract. A second MCP connection's `start_recording` is deliberately allowed to replace the first directory and owner; the first connection then observes that takeover, and its manual `stop_recording` unconditionally stops the replacement. This is upstream behavior rather than a plugin approval or ownership denial. The no-video gate now uses two temporary directories, preserves each local schema-v1 manifest, rejects any mp4, requires distinct non-empty owners, and pins exact start/status/stop Unicode text alongside the eight-field state. Agent guidance still preflights and avoids clobbering unrelated work voluntarily because the daemon itself will not refuse it.
- 2026-07-29: plugin 0.17.25 is verified by GitHub Actions run `30447396606`. ARM64 job `90561022976` and Intel job `90561023089` both passed the exact two-connection recorder takeover, prior-owner unconditional stop, no-video local artifacts, signed-primary/fallback launch gates, and all preceding contracts; Windows and every supported fallback CPython job also passed.
- 2026-07-29: plugin 0.17.26 completes the permission-free signed-primary session lifecycle response contract. The stdio gate pins exact Unicode success/status text and complete structured state for a fresh `auto` session, same-scope idempotent refresh, a different-scope `session_policy_conflict` with no mutation, one permitted explicit desktop escalation, `desktop_already_active` on repetition, exact end, then the public daemon's exact post-end resurrection guard with `{exit_code:1}`. Only lifecycle tools bypass that guard, so an explicit same-ID `start_session` can bind fresh desktop scope with `revived:true` before a final clean end. These are upstream driver state semantics rather than plugin approval boundaries; agents should still prefer unique run IDs unless continuity is intentional.
- 2026-07-29: plugin 0.17.26 is verified by GitHub Actions run `30448283621`. ARM64 job `90563916380` and Intel job `90563916229` both passed exact fresh/idempotent/conflict/escalation/end/guard/revival session lifecycle behavior over the signed-primary stdio MCP, fallback real-GUI execution, and every preceding contract; Windows and every supported fallback CPython job also passed.
- 2026-07-29: plugin 0.17.27 pins the remaining signed-primary cold-launch success presentation without adding a second targeting source. The permission-free stdio gate reconstructs the exact `Launched {name} (pid {pid}) in background.` text from structured content, appends ordered quoted or `(no title)` windows and their IDs only when returned, and pins the exact `get_window_state` hint. The structured launch result and a subsequent fresh full window inventory remain authoritative.
- 2026-07-29: plugin 0.17.27 is verified by GitHub Actions run `30448591842`. ARM64 job `90564917567` and Intel job `90564917604` both passed exact structured-derived cold-launch success text, signed-primary session/discovery/recorder contracts, fallback real-GUI execution, and all preceding gates; Windows and every supported fallback CPython job also passed.
- Remaining hardware gate: obtain a successful live background screenshot/action/re-screenshot loop on an unlocked Mac where CuaDriver.app has Accessibility and Screen Recording. Hosted CI now attempts this automatically only when its signed-driver TCC readback is genuinely ready.
